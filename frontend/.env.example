# Base URL of your backend API.
# Local dev points at your local backend:
VITE_API_BASE_URL=http://localhost:8000

# In production (Netlify), set this to your VM's HTTPS URL, e.g.:
# VITE_API_BASE_URL=https://api.yourdomain.duckdns.org
